$('#$ID$').tagsinput({
	trimValue : true,
	allowDuplicates: false,
	freeInput: $FREE$,
	delimiter: '$DELIMITER$',
	typeahead : {
		delay : $DELAY$,
		items : 20,
		minLength : $MIN$,
		fitToElement : false,
		source : $OPTIONS$
	}
});
