$('#$ID$').tagsinput({
	trimValue : true,
	allowDuplicates: false,
	freeInput: $FREE$,
	typeahead : {
		delay : $DELAY$,
		items : 20,
		minLength : $MIN$,
		fitToElement : false,
		source : $OPTIONS$
	}
});
